package ru.naves.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Appetite-stimulating light theme palette.
 * High-end cooking app aesthetic.
 */
object NavesColors {
    val appBg = Color(0xFFFEF9F3) // Warm Cream/Vanilla
    val bg = Color(0xFFFFFFFF)    // Pure white for cards
    val surface = Color(0xFFFFFFFF)
    val surface2 = Color(0xFFF7F2EB) // Slightly darker cream for contrast
    val line = Color(0xFFEEE5DA)    // Soft fine border
    val text = Color(0xFF2D2926)    // Espresso/Charcoal
    val dim = Color(0xFF7A736E)     // Muted wood tone for secondary text
    val tomato = Color(0xFFD84315)  // Vibrant Tomato Red (Primary Action)
    val tomatoDim = Color(0xFFFFEDE7) // Soft highlight red
    val basil = Color(0xFF43A047)   // Natural Fresh Green
    val basilDim = Color(0xFFE8F5E9)
    val honey = Color(0xFFF9A825)   // Warm Gold
}
